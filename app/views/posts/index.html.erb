<div class="post_cont" >
  <% if user_signed_in? %>
    <p id="notice"><%= notice %></p>
    <%= link_to 'New Post', new_post_path %>
    <% @posts.each do |p| %>
      <%= render partial:"shared/post", locals: { post: p, view: 'index'}  %>
    <% end %>
  <% end %>
</div>