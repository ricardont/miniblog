<% if current_user == @post.user_id  %>
<div class='post_cont'>
	<h1>Editing Post</h1>
	<%= render 'form', post: @post %>
	<%= link_to 'Show', @post %> |
	<%= link_to 'Back', posts_path %>
</div>
<% end %>
